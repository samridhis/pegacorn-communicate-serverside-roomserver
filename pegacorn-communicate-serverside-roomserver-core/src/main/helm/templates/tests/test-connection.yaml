apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "roomserver-backend.fullname" . }}-test-connection"
  labels:
{{ include "roomserver-backend.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "roomserver-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
